import { memo } from "react";

interface TunnelTransitionProps {
  isActive: boolean;
}

export const TunnelTransition = memo((_props: TunnelTransitionProps) => {
  return null;
});

TunnelTransition.displayName = "TunnelTransition";
